# Agent Chatbot Configuration
# Secrets go in secrets.toml (encrypted vault)

[agent]
ID = "agent-chatbot"
ROLE = "chatbot"
VERSION = "1.0.0"

[broker.local]
URL = "ws://localhost:8080/kadi"
NETWORKS = ["chatbot"]

#[broker.remote]
#URL = "wss://broker.dadavidtseng.info/kadi"
#NETWORKS = ["chatbot"]

[bot.discord]
ENABLED = true
USER_ID = "1438685741751210025"
GUILD_ID = ""

[bot.slack]
ENABLED = true
USER_ID = "U09SCDV78AK"
HTTP_PORT = 3700

[logging]
LEVEL = "debug"

[secrets]
VAULTS = ["chatbot"]
KEYS = ["DISCORD_TOKEN", "SLACK_BOT_TOKEN", "SLACK_SIGNING_SECRET"]
