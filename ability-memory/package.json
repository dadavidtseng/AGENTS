{
  "name": "ability-memory",
  "version": "0.0.9",
  "description": "Domain layer for conversational agent memory — thin wrapper over graph-ability",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration"
  },
  "dependencies": {
    "@kadi.build/core": "^0.11.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "ability-graph": "file:../ability-graph",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "tsx": "^4.0.0",
    "vitest": "^3.0.0"
  }
}
