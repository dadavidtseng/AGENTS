{
  "name": "ability-deploy",
  "version": "0.2.0",
  "entrypoint": "index.js",
  "kind": "ability",
  "description": "Unified deployment ability — Akash Network, local Docker/Podman, and container registry management",
  "scripts": {
    "start": "node index.js",
    "serve": "node index.js",
    "serve:broker": "KADI_MODE=broker node index.js",
    "build": "npm run build",
    "setup": "npm install && npm run build"
  }
}
