# Ability ArcadeDB Configuration
# Secrets go in secrets.toml (encrypted vault)

[broker.local]
URL = "wss://broker.dadavidtseng.com/kadi"
NETWORKS = ["arcadedb"]
MODE = "native"

[arcadedb]
USERNAME = "root"
