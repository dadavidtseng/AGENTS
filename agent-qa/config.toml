# Agent QA Configuration

[broker.local]
URL = "ws://localhost:8080/kadi"
NETWORKS = ["qa", "quest", "git", "eval", "global"]

[broker.remote]
URL = "wss://broker.dadavidtseng.info/kadi"
NETWORKS = ["global"]

[agent]
ROLE = "programmer"
