# Agent Producer Configuration
# Non-secret settings — committed to git
# Secrets go in secrets.toml (encrypted vault)
# .env is optional override only (not committed)

[agent]
ID = "agent-producer"
ROLE = "producer"
VERSION = "0.3.0"

[logging]
LEVEL = "debug"

#[broker.local]
#URL = "ws://localhost:8080/kadi"
#NETWORKS = ["chatbot"]

[broker.remote]
URL = "wss://broker.dadavidtseng.info/kadi"
NETWORKS = ["chatbot"]

[bot]
TOOL_TIMEOUT_MS = 10000

[bot.slack]
ENABLED = true
USER_ID = "U09SCDV78AK"

[bot.discord]
ENABLED = true
USER_ID = "1438685741751210025"

[memory]
DATA_PATH = "./data/memory"

[secrets]
VAULTS = ["anthropic", "model-manager"]

[provider]
PRIMARY = "model-manager"
FALLBACK = "anthropic"

[provider.model-manager]
MODEL = "gpt-5-mini"

[provider.anthropic]
MODEL = "claude-haiku-4-5-20251001"
